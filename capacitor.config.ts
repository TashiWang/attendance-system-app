import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ticl Attendance',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
